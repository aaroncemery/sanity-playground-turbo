import {richText} from './rich-text'
import {workflowMetadata} from './workflow-metadata'
import {workflowStatus} from './workflow-status'

export const definitions = [richText, workflowMetadata, workflowStatus]
